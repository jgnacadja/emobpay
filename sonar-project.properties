# must be unique in a given SonarQube instance
sonar.projectKey=emobpay
# --- optional properties ---

# defaults to project key
sonar.projectName=emobpay
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=controllers, views

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8sonar-scanner \
sonar.exclusions=node_modules/**
sonar.host.url=http://codequality.rintio.com
sonar.login=964e89a08fb6f425f5962e1a5a84f76d735cdc51

#sonar.links.ci=https://gitlab.rintio.com/Jgnacadja/sdr/-/pipelines
#sonar.links.issue=https://rintio.atlassian.net/jira/software/projects/SDR/boards/5

# Scan settings.
sonar.projectBaseDir=.
# Define the directories that should be scanned. Comma separated.

# sonar.test.inclusions=**
# sonar.php.coverage.reportPaths=./coverage/lcov.info
# sonar.php.file.suffixes=php
sonar.sourceEncoding=UTF-8

# Fail CI pipeline if Sonar fails.
sonar.qualitygate.wait=true
